# Fixed requirements for Qwen2-VL OCR Service
# Compatible with GTX 1650 and Windows

# Uninstall current PyTorch first: pip uninstall torch torchvision torchaudio -y

# PyTorch with CUDA 11.8 support (compatible with GTX 1650)
# Using newer available versions that work with Qwen2-VL
torch==2.6.0+cu118
torchvision==0.21.0+cu118
torchaudio==2.6.0+cu118
--extra-index-url https://download.pytorch.org/whl/cu118

# Transformers and Qwen dependencies (updated for compatibility)
transformers>=4.45.0
accelerate>=0.25.0
qwen-vl-utils==0.0.14
tokenizers>=0.15.0

# Image processing
opencv-python==4.8.1.78
Pillow==10.0.1
numpy==1.24.4
scikit-image==0.22.0

# Flask web framework
Flask==2.3.3
flask-cors==4.0.0
Werkzeug==2.3.7

# System monitoring and utilities
psutil==5.9.6
requests==2.31.0
regex==2023.10.3

# Additional dependencies for better compatibility
einops==0.7.0
safetensors==0.4.1

# Memory optimization (optional but recommended)
# bitsandbytes==0.41.3