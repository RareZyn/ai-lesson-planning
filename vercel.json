{
  "version": 2,
  "buildCommand": "cd frontend/client && npm install && npm run build",
  "outputDirectory": "frontend/client/build",
  "installCommand": "npm install && cd backend && npm install && cd ../frontend/client && npm install",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    }
  ]
}
