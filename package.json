{
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "start": "node backend/server.js",
    "server": "nodemon server/server.js",
    "client": "cd frontend && cd client && npm run start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": " concurrently \"npm install && cd frontend && cd client && npm install\" \"cd backend && npm install\" "
  }
}
